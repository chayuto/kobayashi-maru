# Task A: Update Package and Prepare

**Goal:** Update bitecs dependency to 0.4.0

**Prerequisites:** None

**Steps:**
1. Edit `package.json` to change `"bitecs": "~0.3.40"` to `"bitecs": "^0.4.0"`
2. Run `npm install` to update the dependencies.

**Files:** 
- `package.json`

**Verification:**
- Run `npm install`.
- Verify the command completes without errors.
- Check `package.json` to ensure the version is updated.

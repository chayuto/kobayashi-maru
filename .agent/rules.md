# Agent Instructions

- **Always run lint and tests before finishing a task.**
- Ensure all code changes are verified with `npm run lint` and `npm test`.
- If tests fail, fix them before notifying the user.
